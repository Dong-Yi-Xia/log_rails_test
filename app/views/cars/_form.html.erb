<%= form_with(model: car) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
        <% car.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label "Car File Picture" %>
    <%= form.file_field :car_picture, direct_upload: true, class: "fileInput" %>

    <% if car.car_picture.attached? %>
      <%= form.label :remove_picture %>
      <%= image_tag @car.car_picture, class: "image_display" %>
      <%= form.check_box :remove_picture %>
    <% end %>
  </div>

  <div class="form-group">
    <%= form.label "Car File Picture" %>
    <%= form.file_field :car_picture, direct_upload: true, class: "fileInput" %>

    <% if car.car_picture.attached? %>
      <%= form.label :remove_picture %>
      <%= image_tag @car.car_picture, class: "image_display" %>
      <%= form.check_box :remove_picture %>
    <% end %>
  </div>

    <div class="form-group">
    <%= form.label "Car File Picture" %>
    <%= form.file_field :car_picture, direct_upload: true, class: "fileInput" %>

    <% if car.car_picture.attached? %>
      <%= form.label :remove_picture %>
      <%= image_tag @car.car_picture, class: "image_display" %>
      <%= form.check_box :remove_picture %>
    <% end %>
  </div>

  <br>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <br>

  <div class="actions">
    <%= form.submit %>
  </div>


<% end %>
